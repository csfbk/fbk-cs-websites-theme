# 0.4.0
* Update collaboration layout (and related list) to reflect new URLs structure
* Update list-collaborations include to support the nameAddon field
* Fix ordered list in internship projects
* Display people's unit and institution only if external
* Show tool's nameAddon as page subtitle
* Enhance visibility for strong elements in dark mode
* Fix issue related to unspecified members' pictures

# 0.3.0
* Hide bio background from personal pages when empty
* Implement common parameters in list-* includes (cols, sort, sortReverse, style)
* Add layouts for lists of collaborations, dissemination, events, projects, publications, teaching, tools (with custom parameters for related includes)
* Add possibility to include custom CSS and JS scripts without affecting those required by the theme (via head-scripts-custom.html and footer-scripts-custom.html)
* Consider project's partners from both projects' YAML and project's page
* Change logic to determine member's FBK role in personal page
* Implement sortNatural parameter in list includes and layouts to allow for selection of sorting operation
* Implement full sorting parameters in list-events include and layout
* Update sortNatural and sortReverse parameters using snake case (sort_natural and sort_reverse)
* Update sort_natural and sort_reverse logic to handle boolean default values through logical instructions instead of the default command
* Change sort parameter in sort_by to avoid conflicts with Liquid method
* Minor fixes

# 0.2.0
* Display social links in header
* Add cols parameter in list-people include
* Fix navbar to display toggle button from mobile devices
* Add config parameter show_tags to show publication tags
* Fix tool layout and list-tools include (typo project -> tool)

# 0.1.0
* Initial release
